se80:herhangi bir objeyi görüntülemeyi sağlar.
Package seçeneğini tıklayalım.
$tmp ile yapılan çalışmaklar gözükür.
zgk_package adını verdim//enter yap nesne oluşturulsun mu der ve evete tıklanır.
package tanımı ister yazınız..
Request istenir..
önceden oluşturduğumuz requestimizi görmek için, own request tıklanır,çift tıklanır ve package hazır.

se38 ile packagemizi deneyelim,
package  z* f4 ile  ismini görebilirsiniz veya adını yazalım...//package ismimiz ekranda yazıyor.
se10 ile request oluşturalım.//Request oluşturalım.
üst sekmede beyaz sayfa tıklanır,
Workbench request seçilir,
Request tanımı yazılır,short description yazılır.

Modifiable ksımında alt alta seçenekler yazar üst kısımdaki user tarafı alt taraftaki ise başkaları girebilir.



Development (d):danışmanların ve modülcülerin kendini geliştirdiği sistem.
Quality (q):kalite sistemi yapılan çalışmanın test edildiği sistem
Production (p):müşterinin kullanmaya başladığı sistem.

Client:var olan bir sistemde farklı bir session açmak.
Request: program yazılır,test edilip onay alın,canlı sisteme geçilir.

se10:kullanıcının requesti görebilir,yeni requestler oluşturabilirim.

Request Type:
Customizing Requests=claint
Workbench Requests= sistem bazında alır
Transport of copies=tek bir çaatı altında toplayan request

